package io.venkat.bookstore.db;

//import com.marklogic.client.DatabaseClient;
//import com.marklogic.client.DatabaseClientFactory;

public class DBConfiguration {
	
	/*private static DatabaseClient mlClient = DatabaseClientFactory.newClient(
		      "localhost", 8000, "8060-patents-modules", "admin", "admin", DatabaseClientFactory.Authentication.DIGEST);
		
	public static DatabaseClient markLogicClient() {
		return mlClient;
	}
*/
}
